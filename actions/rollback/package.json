{
  "name": "autostack-rollback-action",
  "version": "1.0.0",
  "description": "AutoStack Rollback GitHub Action",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist --source-map --license licenses.txt",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "keywords": ["autostack", "rollback", "kubernetes", "github-actions"],
  "author": "AutoStack",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.3.3"
  }
}
