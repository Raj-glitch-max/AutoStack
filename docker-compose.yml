version: '3.8'

services:
  autostack-frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    env_file:
      - .env.docker
    restart: unless-stopped
    networks:
      - autostack-network

networks:
  autostack-network:
    driver: bridge
